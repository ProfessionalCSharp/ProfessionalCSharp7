<Page
    x:Class="DataBindingSamples.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DataBindingSamples"
    xmlns:views="using:DataBindingSamples.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <CommandBar Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <AppBarButton Icon="Refresh" Label="Refresh" Click="{x:Bind RefreshBooks}" />
        </CommandBar>
        <ListView ItemsSource="{x:Bind Books, Mode=OneWay}" SelectedItem="{x:Bind SelectedBook, Mode=TwoWay}" Grid.Row="1" />
        <views:BookUserControl Grid.Row="1" Grid.Column="1" Book="{x:Bind SelectedBook, Mode=OneWay}" />

    </Grid>
</Page>
