<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LayoutSamples"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Class="LayoutSamples.MainPage"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical">
            <Button Content="StackPanel" Click="OnStackPanelPage" />
            <Button Content="Canvas" Click="OnCanvasPage" />
            <Button Content="Grid" Click="OnGridPage" />
            <Button Content="VariableSizedWrapGrid" Click="OnVariableSizedWrapGrid" />
            <Button Content="RelativePanel" Click="OnRelativePanelPage" />
            <Button Content="Adaptive Relative Panel" Click="{x:Bind Path=OnAdaptiveRelativePanelPage}" />
            <Button Content="Delay Loading" Click="{x:Bind Path=OnDelayLoading}" />
        </StackPanel>
    </Grid>
</Page>
