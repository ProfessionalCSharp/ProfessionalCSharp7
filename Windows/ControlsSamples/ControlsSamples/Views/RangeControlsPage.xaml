<Page
    x:Class="ControlsSamples.Views.RangeControlsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ControlsSamples.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <AppBarButton Icon="Back" Click="{x:Bind OnGoBack}" />
        <ScrollBar x:Name="scrollBar" Grid.Row="1" />
        <ProgressBar x:Name="progressBar" Grid.Row="2" />
        <ProgressBar IsIndeterminate="True" Grid.Row="3" />
        <Slider x:Name="slider" Grid.Row="4" />

    </Grid>
</Page>
